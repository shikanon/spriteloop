# SpriteLoop 工作台设计

完整概念：/Users/bytedance/.codex/generated_images/01a0a0ac-e80a-70f0-861c-29e1200f832a/exec-e2db07eb-0b96-41b8-9f16-e02456b08225.png

- 背景 #141619，面板 #1c1f23，边框 #30343a，强调 #c7f96b，正文 #edf0f3，次要文字 #919aa7。
- 字体：Inter / system sans + PingFang SC；数字 ui-monospace。标题 32px、面板 14px、正文 13px、注释 11px。
- 顶栏品牌、动画工作台 / 作品库 / 使用指南；标题「制作你的下一帧。」；三步提示。
- 三栏编辑器：动画设定、动画预览 + 序列帧时间轴、帧处理 + 导出设置。面板 8px 圆角，细描边。控制 6px 圆角。Lucide 16px、1.6px 描边。
- 绿色游侠是单独生成的透明示例图片，无覆盖色；棋盘由 CSS 绘制。默认示例必须标示为静态原画，不能伪装生成结果。时间轴未生成时用空槽。
- 主要工作流：上传首尾帧 / 复用首帧、动作设置、真实方舟异步生成、视频采样、去白底、播放逐帧、导出。附带导入已有视频和本地作品库作为恢复入口。
- 本机优先；仅在用户主动生成时调用付费模型。错误不能伪装为成功。密钥不进入浏览器。

## 生产示例资产

使用内置 Image Gen 从概念提取透明游侠，保存至 `public/assets/ranger.png`。提示词要点：提取概念中的绿色兜帽游侠，保留披风、皮甲、短剑与像素画风，单个完整角色、透明背景、无阴影与文字。

相对概念的功能性调整：静态示例不伪装生成结果；时间轴展示真实帧数；时长与分辨率并排；额外提供已有视频导入与应用处理设置按钮。色彩、三栏、棋盘画布、绿色主操作和图集条的设计保持一致。
